# میکروسرویس تبدیل واحدها (Unit Converter Microservice)

**شماره دانشجویی:** ۱۴۰۱۵۳۶۱۹۲۲  
**پروژه تمرین ۱ مهندسی نرم‌افزار**

این پروژه یک میکروسرویس ساده و قابل اعتماد با **FastAPI** پیاده‌سازی شده که تبدیل واحدهای **طول** و **دما** را انجام می‌دهد.  
تمام الزامات فنی پروژه رعایت شده است:
- کنترل نسخه با Git و استفاده از feature branch + Pull Request
- استقرار با Docker (Dockerfile کامل)
- کامیت‌های معنادار و شفاف

## واحدهای پشتیبانی‌شده

### طول (Length)
- `meter`
- `kilometer`
- `centimeter`
- `inch`
- `foot`

### دما (Temperature)
- `celsius`
- `fahrenheit`
- `kelvin`

## نحوه استفاده از API

### endpoint اصلی: `POST /convert`

**درخواست نمونه:**
```json
{
  "value": 100,
  "from_unit": "celsius",
  "to_unit": "fahrenheit"
}
```

**پاسخ نمونه:**
```json
{
  "result": 212.0,
  "unit": "fahrenheit"
}
```

**مثال‌های بیشتر:**
- ۱ کیلومتر → متر → نتیجه: `1000.0`
- ۳۲ فارنهایت → سلسیوس → نتیجه: `0.0`
- ۰ سلسیوس → کلوین → نتیجه: `273.15`

در صورت خطا (واحد نامعتبر یا ترکیب نادرست)، خطای HTTP 400 با پیام فارسی واضح برمی‌گردد.

مستندات تعاملی کامل (Swagger UI) بعد از اجرای پروژه در آدرس زیر قابل دسترسی است:  
**http://localhost:8000/docs** (بهترین راه برای تست API)

## اجرای پروژه - گام به گام

### روش ۱: اجرای محلی (بدون Docker)

1. پروژه را کلون کنید:
   ```bash
   git clone https://github.com/AysanZ/unit-converter-microservice.git
   cd unit-converter-microservice
   ```

2. (توصیه می‌شود) یک محیط مجازی بسازید:
   ```bash
   python3 -m venv venv
   source venv/bin/activate    # در ویندوز: venv\Scripts\activate
   ```

3. وابستگی‌ها را نصب کنید:
   ```bash
   pip install -r requirements.txt
   ```

4. سرور را اجرا کنید:
   ```bash
   uvicorn app.main:app --reload
   ```

5. در مرورگر به این آدرس بروید:
   **http://127.0.0.1:8000/docs**

6. API را با Try it out تست کنید.

### روش ۲: اجرای با Docker (الزامی پروژه)

1. پروژه را کلون کنید:
   ```bash
   git clone https://github.com/AysanZ/unit-converter-microservice.git
   cd unit-converter-microservice
   ```

2. تصویر Docker را بسازید:
   ```bash
   docker build -t unit-converter .
   ```

3. کانتینر را اجرا کنید:
   ```bash
   docker run -p 8000:8000 unit-converter
   ```

4. در مرورگر به این آدرس بروید:
   **http://localhost:8000/docs**

5. API کاملاً ایزوله و مستقل اجرا می‌شود و دقیقاً مثل حالت محلی کار می‌کند.

## تست‌های پیشنهادی (در صفحه /docs)

- دما: ۱۰۰ سلسیوس → فارنهایت → انتظار: `212.0`
- دما: ۳۲ فارنهایت → سلسیوس → انتظار: `0.0`
- طول: ۱۰۰ سانتی‌متر → متر → انتظار: `1.0`
- طول: ۱ فوت → اینچ → انتظار: `12.0`
- خطا: سلسیوس → متر → باید خطای ۴۰۰ بدهد

## ساختار پروژه
```
.
├── app/
│   └── main.py              # کد اصلی سرویس
├── Dockerfile               # برای ساخت و اجرای کانتینر
├── requirements.txt         # وابستگی‌های پایتون
├── README.md                # توضیحات کامل پروژه
└── .gitignore
```

پروژه با رعایت کامل اصول مهندسی نرم‌افزار (Git branching، Conventional Commits، Dockerization) توسعه داده شده است.

**موفق باشید!** 🚀
